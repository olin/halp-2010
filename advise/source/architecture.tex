\section{Rules Architecture}

Rules are specnfied by the following fields:
\begin{itemize}
\item Name: the name of the rule (used internally)
\item Category: the rule group this rule belongs to
\item Triggers: the key that specifies which course or compound will trigger a re-evaluation of this rule. Also controls the search string.
\item Code: the display 
\end{itemize}